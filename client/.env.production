# Backend API URL — update this when the backend is deployed (e.g. Railway)
# Example: VITE_API_URL=https://jhesster-backend.up.railway.app
# Leave blank until the backend is live — auth/online features will be unavailable
VITE_API_URL=
